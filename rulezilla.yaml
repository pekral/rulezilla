commands:
  phpstan:
    directories: ['src', 'tests']
    config: phpstan.neon
  phpcs:
    directories: ['src', 'tests']
    ruleset: ruleset.xml
  rector:
    directories: ['src', 'tests']
    ruleset: rector.php